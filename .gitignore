# ── INTRANET: archivos que viven solo en el servidor ──
intranet/setup.php
intranet/setup.lock
intranet/db.php
intranet/assets/*
!intranet/assets/.htaccess

# ── CATÁLOGOS PDF (fuente, no sube al repo) ──
*.pdf

# ── EDITORES ──
.vscode/
.idea/
*.swp
*.swo
*.bak

# ── ENTORNO ──
.env
.env.*
*.env

# ── LOGS ──
*.log

# ── SISTEMA OPERATIVO ──
.DS_Store
Thumbs.db
desktop.ini
