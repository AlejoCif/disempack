# Setup — se sube una sola vez manualmente por File Manager, nunca via deploy
intranet/setup.php

# Lock file generado en el servidor — nunca debe pisarse con un deploy
intranet/setup.lock

# Imágenes subidas por la intranet (viven solo en el servidor)
intranet/assets/*
!intranet/assets/.htaccess

# Mac
.DS_Store
